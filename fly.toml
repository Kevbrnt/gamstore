app = 'gamestoreprojet'
primary_region = 'cdg'

[build]
dockerfile = 'build/Dockerfile'

[http_service]
internal_port = 80
force_https = true
auto_stop_machines = true
auto_start_machines = true
min_machines_running = 0

[[vm]]
memory = '2gb'
cpu_kind = 'shared'
cpus = 1